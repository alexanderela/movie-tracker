import React from 'react';
import { shallow } from 'enzyme';
import { MainPage } from '../';

describe('MainPage', () => {
  beforeEach(() => {
  });

  it('Should be tested', () => {
    expect(true).toBe(true);
  });
});
